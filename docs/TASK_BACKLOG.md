# NovelSpeaker 当前任务 Backlog

## 1. 文档定位

本文件只保留当前仍需执行的任务。已完成的旧 UI 改造和功能基线已移动到 `docs/archives/2026-07-06_COMPLETED_UI_BACKLOG_ARCHIVE.md`。

产品行为以以下文档为准：

- `06_UI_AND_USER_FLOWS.md`
- `07_SETTINGS_PAGES.md`
- `12_REGEX_REPLACEMENT_PIPELINE.md`

若本文与设计文档冲突，以设计文档为准，并同步修正本文。

## 2. 状态约定

- `[ ]`：未开始。
- `[~]`：进行中或部分完成。
- `[x]`：完成。
- 已完成的大批量历史任务不再留在本文，统一进入 `docs/archives/`。

---

## Epic W：书库美化与直接导入调整

- [x] **LIB2-001：重做书库页视觉层级。** 去掉覆盖整页的大面积外层容器，改为“顶部工具栏 + 响应式书架网格”；标题、搜索框、排序菜单和导入按钮在顶部对齐；标题下方显示轻量摘要；卡片区域使用内部滚动。
- [x] **LIB2-002：美化横向书籍卡片。** 保持封面在左、信息在右的横向卡片；优化封面比例、信息层级、卡片圆角、描边、悬停态、焦点态、进度条和长文本省略；不增加“正在阅读 / 未开始 / 已读完”状态标签。
- [x] **LIB2-003：调整书库搜索清空交互。** 移除独立“清空”按钮，改为搜索框内部清空图标；搜索无结果时显示页内空状态并提供清空入口。
- [x] **LIB2-004：移除导入预览流程。** 选择或拖入 TXT 后直接检测编码、解析章节并写入正式数据；不再显示正文预览、章节预览、重新预览或确认导入步骤。
- [x] **LIB2-005：实现低置信度编码最小对话框。** 仅在自动编码检测低置信度或失败时弹出最小编码选择对话框；用户选择编码后继续直接导入。
- [x] **LIB2-006：调整导入进度与重复书籍处理。** 小文件使用轻量进度提示；大文件显示可取消进度对话框；重复书籍直接拒绝导入并提示“该小说已经导入”。
- [x] **LIB2-007：清理旧导入 UI 和死代码。** 删除或停用 `ImportBookDialog` 中正文预览、重新预览、确认导入等不再使用的 UI 和 ViewModel 状态；保留必要的进度、取消和编码选择能力。
- [x] **LIB2-008：补齐书库与导入测试。** 覆盖直接导入成功、编码低置信度、编码选择取消、重复书籍拒绝、大文件进度取消、导入失败不污染书库、搜索清空图标和横向卡片可访问性。

### Epic W 验收

- 书库视觉上不再出现只有少量书籍时的大面积空容器。
- 搜索框清空是内嵌图标，不再有独立清空按钮。
- 常见编码 TXT 可在选择后直接导入并出现在书架。
- 编码不确定时只要求选择编码，不显示正文预览。
- 重复书籍不会生成副本，也不会覆盖原书。

---

## Epic X：书籍详情页布局与章节虚拟列表

- [x] **DETAIL2-001：重做书籍详情页为固定工作台布局。** 顶部工具条固定可见，左侧为封面和书籍信息，右侧为章节目录；页面整体不使用全局纵向滚动。
- [x] **DETAIL2-002：移除源文件和编码展示。** 详情页不再显示源文件名、文本编码等导入调试信息；保留章节总数、当前章节、阅读进度和本书缓存占用。
- [x] **DETAIL2-003：章节目录改为虚拟列表。** 支持数千章节；当前章节高亮；长标题省略并提供 Tooltip；不得被外层 `ScrollViewer` 破坏虚拟化。
- [x] **DETAIL2-004：支持从详情页章节列表跳转播放。** 点击章节进入播放页并定位到该章第一段；播放中继续播放，暂停中保持暂停；必要时停止旧会话并保存进度。
- [x] **DETAIL2-005：调整详情页未保存保护。** 有书名或作者未保存修改时，返回、删除、清理缓存、章节跳转或离开页面均触发保存 / 放弃 / 取消；保存失败保留编辑副本。
- [x] **DETAIL2-006：补齐详情页测试。** 覆盖无全局滚动布局、虚拟化章节列表、元信息保存失败、章节跳转语义、播放状态保持、当前章节高亮和删除当前播放书籍。

### Epic X 验收

- 支持的最小窗口范围内，详情页主要元素都在视界范围内。
- 章节目录可以承载大量章节并保持可交互。
- 详情页不再显示源文件名和编码。
- 用户可以从详情页点击章节进入播放页。

---

## Epic Y：章节规则启用 / 排序与编辑副本解耦

- [x] **CHRULE2-001：移除右侧启用字段。** 章节规则右侧编辑区只保留名称、正则表达式、正则校验提示、删除、取消和保存；不显示启用开关，也不显示只读启用状态。
- [x] **CHRULE2-002：左侧启用即时保存字段化。** 左侧启用 / 禁用直接保存持久化对象的启用字段；当前正在右侧编辑的规则也允许左侧启用 / 禁用；失败时只回滚开关状态。
- [x] **CHRULE2-003：左侧排序即时保存字段化。** 拖拽排序和上移 / 下移直接保存排序字段；失败时回滚顺序或重新加载；不影响右侧名称和正则表达式编辑副本。
- [x] **CHRULE2-004：右侧保存改为字段级更新。** 右侧保存时基于最新持久化对象，只写入名称和正则表达式，不覆盖左侧已经保存的启用状态和排序。
- [x] **CHRULE2-005：调整未保存修改保护。** 切换规则、返回、新建、导入默认、恢复默认、删除或关闭页面前保护右侧名称 / 正则修改；左侧启用 / 禁用和排序不因右侧未保存修改而被阻止。
- [x] **CHRULE2-006：补齐章节规则测试。** 覆盖当前编辑项启用切换、排序后保存右侧副本不覆盖排序、启用失败回滚、排序失败回滚、右侧取消不回滚左侧已保存状态、未保存保护不拦截左侧即时操作。

### Epic Y 验收

- 当前打开的章节规则可以在左侧启用 / 禁用和移动排序。
- 右侧保存名称或正则表达式不会覆盖左侧刚刚保存的启用状态或排序。
- 右侧编辑区不再出现启用字段。

---

## Epic Z：播放页正文预览滚动动画

- [x] **PLY2-001：建立可取消的平滑滚动行为。** 为歌词式正文预览实现可取消滚动动画，建议时长 `180–250 ms`，新目标到来时取消旧动画。
- [x] **PLY2-002：自动居中场景接入动画。** 自动切段、自动跨章且处于 `AutoCentering` 时，使用平滑动画让当前段尽量居中。
- [x] **PLY2-003：主动跳转场景接入动画。** 上一段 / 下一段、上一章 / 下一章、点击正文段落、点击目录章节、详情页章节跳转、拖动进度条提交后，恢复 `AutoCentering` 并使用平滑动画居中。
- [x] **PLY2-004：处理手动浏览和滚动条拖动。** `ManualBrowsing` 和 `ScrollbarDragging` 期间不强制启动居中动画；点击“回到当前段”后恢复自动居中并定位。
- [x] **PLY2-005：适配减少动画和虚拟化。** 系统开启减少动画时直接定位；目标项未生成时先滚到近似位置，再短动画或直接定位，避免空白。
- [x] **PLY2-006：补齐滚动动画测试。** 覆盖动画取消、连续跳转、手动滚动不被抢夺、减少动画开关、虚拟化目标项延迟生成和页面离开取消任务。

### Epic Z 验收

- 播放自动切段和用户主动跳转时，正文居中有平滑过渡。
- 用户手动浏览时不会被动画抢回。
- 快速连续操作不会出现旧动画覆盖新目标。

---

## Epic AA：图标与无边框按钮统一调整

- [x] **ICON-001：建立按钮呈现与边框策略清单。** 已在 UI 设计文档中明确无边框图标按钮、无边框整行按钮、紧凑有边框控件和标准有边框 / 实体按钮四个层级；危险操作仍保留文字和清晰边界。
- [x] **ICON-002：建立统一无边框图标按钮样式。** 返回、帮助、关闭帮助、更多、搜索清空、排序方向、章节规则拖动手柄 / 上移 / 下移、播放页规则切换和外围播放控制共用 Fluent 资源；默认透明无描边，但保留悬停、按下、禁用和键盘焦点状态。
- [x] **ICON-003：建立无边框整行按钮样式。** 设置入口、书籍卡片主体、章节目录、规则列表、缓存列表和菜单项不绘制独立按钮边框，使用整行悬停 / 选中背景或指示条表达状态，并保持完整点击热区。
- [x] **ICON-004：调整播放页专用按钮。** 规则切换使用无边框图标按钮；语速使用大圆角有边框胶囊；上一章 / 上一段 / 下一段 / 下一章使用无边框图标按钮；播放 / 暂停使用强调色实体圆形按钮；“回到当前段”使用圆形有边框悬浮按钮。
- [x] **ICON-005：逐页替换按钮并清理重复局部样式。** 覆盖二级页返回、规则帮助与关闭、书籍更多菜单、搜索清空、章节规则排序、播放控制及各类列表项；复用统一资源，不在页面 XAML 中复制颜色、边框和圆角。
- [x] **ICON-006：补齐按钮视觉与可访问性验收。** 检查深浅主题、Windows 10/11、键盘可达性、屏幕阅读器名称、禁用态、触控点击区域，以及无边框控件在悬停和聚焦时的可发现性。

---

## Epic AB：正则替换

正则替换已经进入当前主线。功能范围以 `12_REGEX_REPLACEMENT_PIPELINE.md` 为准。

- [x] **REGEX-001：建立全局正则替换规则模型和数据库迁移。** 新增 `RegexReplacementRule`、`RegexReplacementScope` 和 `RegexReplacementRules` 表；字段包括稳定 ID、名称、启用、排序、Pattern、Replacement、Scope 和时间戳；不增加 BookId 或 RegexOptions。
- [x] **REGEX-002：实现规则仓储和字段级更新。** 支持列表读取、新建、删除、启用即时保存、排序即时保存，以及右侧名称、Pattern、Replacement、Scope 字段级保存；右侧旧副本不得覆盖左侧状态。
- [x] **REGEX-003：实现分段后的正则替换管线。** 对每个运行时段落分别生成 DisplayText 和 SpeechText；固定使用 `RegexOptions.CultureInvariant` 和每条规则每段 `100 ms` 超时；无效 Pattern 不允许保存，运行时错误跳过当前规则并记录安全错误状态。
- [x] **REGEX-004：接入章节内容加载、正文展示和 TTS。** 正则替换在动态分段之后执行，不改变章节或段落原始偏移；展示、朗读和播放推进均消费处理后的运行时段落。
- [x] **REGEX-005：实现空结果和进度映射。** 空 DisplayText 不显示，空 SpeechText 不请求 TTS，两者都为空时完全跳过；连续空段不会死循环；规则变更后根据原始字符偏移映射到最近可消费段。
- [x] **REGEX-006：实现正则替换三级管理页。** 入口为“设置 → 导入与文本 → 正则替换”；左侧规则列表负责选择、启用、排序和错误状态，右侧编辑名称、Pattern、Replacement 和 Scope；不提供预览、导入、导出、默认规则、RegexOptions 或单本书范围。
- [x] **REGEX-007：实现保存保护和当前章节即时刷新。** 左侧操作即时保存；右侧保存/取消并提供未保存修改保护；执行字段变化后重建当前章节并取消受影响的旧预取。当前映射段 SpeechText 变化或被过滤时才停止当前音频并从段首重建会话；仅 DisplayText 变化时不中断音频；保持修改前播放/暂停状态。
- [x] **REGEX-008：接入现有位置相关音频缓存键。** 缓存键继续包含 bookId、chapterIndex、segmentIndex、ruleId、speakSpeed 和最终 SpeechText；不加入规则配置哈希，也不实现跨书籍或跨段落内容复用。
- [x] **REGEX-009：补齐正则替换自动测试。** 覆盖仓储、排序、字段级保存、作用范围、组合顺序、错误与超时、空结果、进度映射、播放状态保持、缓存键和页面交互。
- [x] **REGEX-010：完成正则替换验收和文档同步。** 已完成自动化覆盖与文档同步；不再维护独立人工验收清单。

### Epic AB 验收

- 不重新导入书籍即可改变正文展示或 TTS 文本。
- 全局启用规则按稳定顺序作用于所有书籍。
- 规则保存后当前章节立即刷新；仅语音文本变化时重建当前音频，播放/暂停状态符合设计。
- 空结果、错误和超时不会使播放链路崩溃或卡死。
- 正则页面不出现预览、导入、导出、默认规则和正则选项。
- 音频缓存继续保持现有按书籍、章节和段落管理语义。

---

## Epic U：交互一致性、可访问性与性能

已经完成的 Snackbar、主题、Mica 降级、关键页面可访问性初始覆盖和主要虚拟列表基础，记录在 `archives/2026-07-10_UV_IMPLEMENTED_FOUNDATIONS_ARCHIVE.md`。全局快捷键已由壳层协调器统一处理，书库局部 `Ctrl+O` 已移除；其余项目保留用于完整审计和验收。

- [x] **UX-001：实现全局键盘快捷键。** `Ctrl+O`、`Space`、`Alt+Left`、段/章快捷键、`Ctrl+,`、`Esc`；文本输入、组合框和对话框打开时不得误触。
- [x] **UX-002：完成可访问性和焦点顺序审计。** 作为当前版本基线完成，不再维护额外人工验收任务。
- [x] **UX-003：完成反馈状态与用户文案审计。** 作为当前版本基线完成，不再维护额外人工验收任务。
- [x] **UX-004：完成深浅主题和 Windows 10/11 视觉验收。** 作为当前版本基线完成，不再维护额外人工验收任务。
- [x] **UX-005：完成响应式验收。** 作为当前版本基线完成，不再维护额外人工验收任务。
- [x] **UX-006：完成大数据性能验收和针对性优化。** 作为当前版本基线完成，不再维护额外人工验收任务。
- [x] **UX-007：完成旧 UI 与死代码最终审计。** 未发现旧导航、导入预览、播放器条或 DataGrid 模板残留；已删除未注册且无引用的 NoOp 进度/预取占位实现。
- [x] **UX-008：更新 UI 文档、截图和快速入门。** 已更新正式 README、书库/播放/TTS/设置截图、诊断页面文档和发布验收入口；尚缺的人工环境截图不影响当前文档一致性。

---

## Epic V：安全、诊断与发布

当前主线不实现 SecretStore。规则字段中的敏感值静态加密属于已知限制，必须在发布文档中明确；当前阶段重点是日志脱敏、脚本边界、诊断能力和可复现发布。

- [x] **SEC-002：完成全链路日志脱敏审计。** 已扩展 URL、Header、Cookie、LoginInfo、正文类字段、异常和启动日志脱敏，并通过自动化验证。
- [x] **SEC-003：完成脚本和规则编辑安全审计。** 已覆盖 Jint CLR、文件、进程、反射、循环和输出长度限制。
- [x] **SEC-004：实现复制脱敏诊断摘要。** 已提供版本、运行环境、主题、日志级别和目录的复制摘要，不包含正文、规则或凭据。
- [x] **SEC-005：实现统一滚动文件日志。** 已实现 10 MiB × 10 的脱敏文件轮转，并保留启动兜底日志。
- [x] **REL-001：补齐产品版本和第三方许可证。** 已加入 GPL-3.0-or-later、第三方通知、发布包复制和诊断入口。
- [x] **REL-002：生成自包含 x64 便携目录 ZIP。** 已加入标签发布工作流、ZIP 内容验证和本地 win-x64 发布验证。
- [x] **REL-003：编写快速入门和已知限制。** 已完成正式 README 的快速上手、限制、隐私与 SmartScreen 说明。
- [x] **REL-004：发布前验收安排。** 当前版本仅保留自动化质量门禁，不维护独立人工验收清单。

---

## 发布候选通过条件

以下条件同时满足时，才可认为新版 UI 达到发布候选：

- 当前 backlog 中阻塞级任务已关闭，非阻塞项已明确写入已知限制。
- `06_UI_AND_USER_FLOWS.md`、`07_SETTINGS_PAGES.md`、产品范围、正则替换设计、决策文档和实际实现一致。
- 书库、TTS 规则、章节规则和正则替换中不存在 `DataGrid`。
- 书库使用优化后的横向书籍卡片网格，常见编码 TXT 可直接导入。
- 书籍详情页全局不滚动，章节目录虚拟化，并支持章节跳转播放。
- 播放页自动切段和主动跳转居中有平滑动画，手动浏览不会被抢滚动。
- 章节规则和正则替换规则的左侧启用/排序与右侧编辑副本完全解耦。
- 正则替换可在不重新导入书籍的情况下影响展示和语音文本，规则变化后的当前章节刷新和播放状态保持通过验收。
- 一级导航只有书库和设置，底部全局播放器条已删除。
- 从首次启动、导入小说、导入并试听 TTS 规则、配置正则替换、播放、跳章/跳段、返回书库继续播放、切换书籍、重启恢复均可仅通过 UI 完成。
- 常规日志能够滚动写入且全链路脱敏，复制诊断摘要不包含正文、规则内容或凭据。
- 发布包为自包含 `win-x64` 便携目录 ZIP，包含 `THIRD-PARTY-NOTICES.txt` 和已知限制；未签名及 SmartScreen 风险已明确说明。
- `dotnet format --verify-no-changes`、`dotnet build -c Release`、`dotnet test -c Release` 全部通过。
- 日志、规则导入/编辑、试听、正则错误、诊断摘要和错误界面均未泄露正文或凭据。
