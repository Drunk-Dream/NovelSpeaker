# 2026-07 架构重构阶段完成摘要

本文件归档上一阶段 `TASK_BACKLOG.md` 的结果。归档只用于追溯，不作为当前设计或开发计划依据。

## 阶段目标

上一阶段围绕既有产品行为完成了分层和可维护性重构，重点包括：

- 恢复 Domain、Application、Infrastructure、App 的职责边界。
- 将导入、章节规则、正则替换、TTS、缓存和播放用例逐步迁移到 Application。
- 拆分播放协调、页面投影、导航、页面激活和启动生命周期。
- 按职责拆分测试项目，建立架构依赖、故障恢复和发布内容检查。
- 清除正式发布包中的测试资产、重复入口、迁移适配器和已确认的死代码。
- 保持既有 SQLite migration、应用数据、正文存储、阅读进度和音频缓存兼容。

## 完成结果

原 Wave 0–9 已全部完成，包括最后的发布验证：

- `VERIFY-903` 的自动门禁与 self-contained `win-x64` publish 检查已完成。
- 自动测试记录为 5 个测试项目、737 项测试全部通过，失败 0、跳过 0。
- 发布目录与 ZIP 已确认不包含测试音频或 `TestAssets`。
- 用户已于 2026-07-26 确认完成剩余手动验证，因此上一阶段关闭。

## 下一阶段继承的技术债

上一阶段 `CLEAN-901` 识别、但有意未在架构迁移中顺手修改的事项，转入新的产品完善阶段：

1. 进一步收紧 DI 与 `IServiceProvider` 解析边界。
2. 统一 Page activation、操作取消、导航守卫和后台任务所有权。
3. 明确 HTTP response、NAudio 和缓存临时文件的资源释放/失败清理责任。
4. 将 TTS 限流改为真正异步的共享并发控制，并整理 SQLite 时间/序列化容错。
5. 清理旧测试命名空间、重复 TestKit、低价值重复测试和历史兼容 API。
6. 继续收缩暴露 WPF 视觉类型或旧兼容属性的公共 Presentation 合同。

这些事项已重新拆分到当前 `../TASK_BACKLOG.md`，不应从本归档恢复旧执行顺序。