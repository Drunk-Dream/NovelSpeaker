<UserControl x:Class="NovelSpeaker.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{StaticResource PageTitleTextBlockStyle}"
                   Text="设置" />

        <StackPanel Grid.Row="1"
                    Margin="{StaticResource SectionSpacing}">
            <TextBlock Style="{StaticResource SectionTitleTextBlockStyle}"
                       Text="播放" />
            <StackPanel Margin="{StaticResource ItemSpacing}"
                        Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center"
                           Text="默认语速" />
                <TextBox Width="120"
                         Margin="12,0,0,0"
                         Text="{Binding DefaultSpeakSpeed, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="{StaticResource ItemSpacing}"
                        Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center"
                           Text="预取段落数" />
                <TextBox Width="120"
                         Margin="12,0,0,0"
                         Text="{Binding PrefetchCount, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="{StaticResource ItemSpacing}"
                        Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center"
                           Text="日志级别" />
                <ComboBox Width="180"
                          Margin="12,0,0,0"
                          ItemsSource="{Binding AvailableLogLevels}"
                          SelectedItem="{Binding SelectedLogLevel}" />
            </StackPanel>

            <StackPanel Margin="{StaticResource ItemSpacing}"
                        Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center"
                           Text="主题" />
                <ComboBox Width="180"
                          Margin="12,0,0,0"
                          ItemsSource="{Binding AvailableThemes}"
                          SelectedItem="{Binding SelectedTheme}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Margin="{StaticResource SectionSpacing}">
            <TextBlock Style="{StaticResource SectionTitleTextBlockStyle}"
                       Text="导入与文本" />
            <CheckBox Margin="{StaticResource ItemSpacing}"
                      Content="启用超长段落拆分"
                      IsChecked="{Binding EnableLongParagraphSplitting}" />

            <StackPanel Margin="{StaticResource ItemSpacing}"
                        Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center"
                           Text="拆分阈值" />
                <TextBox Width="120"
                         Margin="12,0,0,0"
                         Text="{Binding LongParagraphThreshold, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="{StaticResource ItemSpacing}">
                <TextBlock Text="文件名模板" />
                <TextBox Margin="0,8,0,0"
                         Text="{Binding BookFileNameTemplate, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Margin="0,6,0,0"
                           Style="{StaticResource SecondaryTextBlockStyle}"
                           Text="{}示例：{{name}} 作者：{{author}}；留空则禁用模板解析。" />
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="3"
                Width="120"
                Margin="{StaticResource SectionSpacing}"
                HorizontalAlignment="Left"
                Command="{Binding SaveCommand}"
                Content="保存设置" />

        <TextBlock Grid.Row="4"
                   Margin="0,16,0,0"
                   Style="{StaticResource SecondaryTextBlockStyle}"
                   Text="{Binding StatusMessage}" />

        <Border Grid.Row="5"
                Margin="{StaticResource SectionSpacing}"
                Padding="12"
                Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitleTextBlockStyle}"
                           Text="管理入口" />
                <WrapPanel Margin="{StaticResource ItemSpacing}">
                    <Button Margin="0,0,12,12"
                            Padding="18,10"
                            Command="{Binding OpenTtsRulesCommand}"
                            Content="TTS 规则" />
                    <Button Margin="0,0,12,12"
                            Padding="18,10"
                            Command="{Binding OpenChapterRulesCommand}"
                            Content="章节规则" />
                    <Button Margin="0,0,12,12"
                            Padding="18,10"
                            Command="{Binding OpenCacheManagementCommand}"
                            Content="缓存管理" />
                </WrapPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
