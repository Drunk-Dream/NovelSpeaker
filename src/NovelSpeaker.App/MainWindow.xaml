<Window x:Class="NovelSpeaker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NovelSpeaker"
        Height="760"
        Width="1180">
    <Grid Background="#FFF5F2EA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24" Background="#FFF0E5D1">
            <StackPanel Orientation="Horizontal">
                <Button Margin="0,0,12,0" Content="书库" Click="LibraryButton_OnClick" />
                <Button Margin="0,0,12,0" Content="播放" Click="PlayerButton_OnClick" />
                <Button Margin="0,0,12,0" Content="规则" Click="RulesButton_OnClick" />
                <Button Content="设置" Click="SettingsButton_OnClick" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="24" Padding="24" Background="White" CornerRadius="8">
            <ContentControl Content="{Binding CurrentPage}" />
        </Border>

        <Border Grid.Row="2" Padding="20" Background="#FF1D3557">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Foreground="White"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Text="{Binding Player.CurrentTitle}" />
                    <TextBlock Foreground="#FFD8E2EC"
                               Text="{Binding Player.StatusText}" />
                    <TextBlock Foreground="#FFBFD3E6"
                               Text="{Binding Player.DetailText}" />
                    <TextBlock Foreground="#FFBFD3E6"
                               Text="{Binding Rules.CurrentRuleDisplayText}" />
                </StackPanel>
                <Button Grid.Column="1"
                        MinWidth="120"
                        Margin="0,0,12,0"
                        Padding="20,10"
                        Command="{Binding Player.TogglePlayPauseCommand}"
                        Content="{Binding Player.PrimaryActionText}" />
                <Button Grid.Column="2"
                        MinWidth="100"
                        Padding="20,10"
                        Command="{Binding Player.StopCommand}"
                        Content="停止" />
            </Grid>
        </Border>
    </Grid>
</Window>
