namespace NovelSpeaker.Domain.Books;

/// <summary>Specifies which runtime text projection a replacement rule changes.</summary>
public enum RegexReplacementScope
{
    Display,
    Speech,
    Both
}
